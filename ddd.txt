
public class UserService {
    private UserDao userDao;

    public UserService() {
        
        this.userDao = new UserDao();
    }

   
    public List<User> getAllUsers() {
        return userDao.getAllUsers();
    }

    
    public void addUser(User user) {
       
        userDao.addUser(user);
    }
 public class Main {
	    public static void main(String[] args) {
	          Connection connection = null;
 
	        try {
	            // Carregar o driver JDBC
	            Class.forName("com.mysql.cj.jdbc.Driver");
 
	            // URL de conexão com o banco de dados
	            String url = "jdbc:mysql://localhost:3306/seu_banco";
 
	            // Usuário e senha do banco de dados
	            String user = "seu_usuario";
	            String password = "sua_senha";
 
	            // Estabelecer a conexão com o banco de dados
	            connection = DriverManager.getConnection(url, user, password);
 
	            // Verificar se a conexão foi estabelecida com sucesso
	            if (connection != null) {
	                System.out.println("Conexão bem sucedida!");
	            } else {
	                System.out.println("Falha na conexão.");
	            }
 
	        } catch (ClassNotFoundException e) {
	            System.out.println("Driver JDBC não encontrado: " + e.getMessage());
	        } catch (SQLException e) {
	            System.out.println("Erro ao conectar ao banco de dados: " + e.getMessage());
	        } finally {
	            // Fechar a conexão
	            if (connection != null) {
	                try {
	                    connection.close();
	                } catch (SQLException e) {
	                    System.out.println("Erro ao fechar a conexão: " + e.getMessage());
	                    
	                    package bo;
 
	                    import java.util.List;
	                    import java.sql.Connection;
	                    import java.sql.PreparedStatement;
	                    import java.sql.ResultSet;
	                    import java.sql.SQLException;
 
	                    import database.DBConnection;
	                    import exceptions.OceanException;
	                    import model.Projeto;
 
	                    public class ProjetoBO {
	                        public List<Projeto> getAllProjetos() throws OceanException {
	                            // Lógica para selecionar todos os projetos do banco de dados
	                        }
 
	                        public void insertProjeto(Projeto projeto) throws OceanException {
	                            // Lógica para inserir um projeto no banco de dados
	                        }
 
	                        public void deleteProjeto(int id) throws OceanException {
	                            // Lógica para excluir um projeto do banco de dados
	                        }
 
	                        public void updateProjeto(Projeto projeto) throws OceanException {
	                            // Lógica para atualizar um projeto no banco de dados
	                        }
	                    }
 
	                    
	                }
	            }
	        }
	    }
	}
 
   
